import 'package:dart_application_semiprime_numbers/dart_application_semiprime_numbers.dart';
import 'package:test/test.dart';

void main() {
  test('calculate', () {
    expect(calculate(), 42);
  });
}
